/**
  * 接口域名的管理
  */
 const domain = 'http://192.168.16.228:8888';
 const base = {
    nvrs: `${domain}/warehouse/nvrs`,
    nvr: `${domain}/warehouse/nvr`,
    camera:`${domain}/warehouse/camera`
}

export default base;